import Vue from 'vue'
import Vuex from 'vuex'
//用户信息
import userInfo from './module/userInfo'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    userInfo
  }
})
